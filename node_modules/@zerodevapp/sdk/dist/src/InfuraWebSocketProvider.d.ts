import { Networkish } from '@ethersproject/networks';
import { ethers } from 'ethers';
declare const InfuraWebsocketProvider_base: new () => ethers.providers.InfuraWebSocketProvider;
export declare class InfuraWebsocketProvider extends InfuraWebsocketProvider_base {
    private provider?;
    private events;
    private requests;
    readonly handler: {
        get(target: InfuraWebsocketProvider, prop: string, receiver: unknown): any;
    };
    constructor(network: Networkish, apiKey: string);
    private create;
}
export {};

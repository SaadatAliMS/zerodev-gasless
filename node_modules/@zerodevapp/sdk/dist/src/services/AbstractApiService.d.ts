declare abstract class AbstractApiService {
    abstract baseUrl: string;
    initiated: boolean;
    headers: {
        [k: string]: string;
    };
    constructor(headers?: {
        [k: string]: string;
    });
    protected init(): Promise<void>;
    protected request(url: string, method: string, params?: {
        [k: string]: string;
    }, body?: any): Promise<Response>;
}
export default AbstractApiService;

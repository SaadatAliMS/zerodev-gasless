import { AssetTransfer } from '../ZeroDevSigner';
import AbstractSapiService from './AbstractSapiService';
declare class MoralisApiService extends AbstractSapiService {
    baseUrl: string;
    static singleton: MoralisApiService;
    constructor(headers?: ConstructorParameters<typeof AbstractSapiService>[0]);
    convertChainIdToMoralisChainId(chainId: number): string | undefined;
    getNativeBalance(chainId: number, address: string): Promise<AssetTransfer | undefined>;
    getTokenBalances(chainId: number, address: string): Promise<AssetTransfer[] | undefined>;
    getNFTBalances(chainId: number, address: string): Promise<AssetTransfer[] | undefined>;
}
export default MoralisApiService;

import { UserOperationStruct } from '@zerodevapp/contracts';
import { Signer } from 'ethers';
import { Bytes } from 'ethers/lib/utils';
import { BaseValidatorAPI, ValidatorMode, BaseValidatorAPIParams } from './BaseValidator';
export interface ECDSAValidatorParams extends BaseValidatorAPIParams {
    owner: Signer;
}
export declare class ECDSAValidator extends BaseValidatorAPI {
    owner: Signer;
    mode: ValidatorMode;
    constructor(params: ECDSAValidatorParams);
    signer(): Promise<Signer>;
    getEnableData(): Promise<string>;
    signUserOp(userOperation: UserOperationStruct): Promise<string>;
    signMessage(message: Bytes | string): Promise<string>;
}

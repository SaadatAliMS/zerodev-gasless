import { UserOperationStruct } from '@zerodevapp/contracts';
import { Bytes, Signer } from 'ethers';
import { BaseValidatorAPI, BaseValidatorAPIParams } from './BaseValidator';
export interface EmptyValidatorParams extends BaseValidatorAPIParams {
    enableData: string;
}
export declare class EmptyValidator extends BaseValidatorAPI {
    enableData: string;
    constructor(params: EmptyValidatorParams);
    static fromValidator(validator: BaseValidatorAPI): Promise<EmptyValidator>;
    signer(): Promise<Signer>;
    getEnableData(): Promise<string>;
    signUserOp(_userOperation: UserOperationStruct): Promise<string>;
    signMessage(message: Bytes | string): Promise<string>;
}

import { UserOperationStruct } from '@zerodevapp/contracts';
import { Signer } from 'ethers';
import { Bytes } from 'ethers/lib/utils';
import { BaseValidatorAPI, BaseValidatorAPIParams } from './BaseValidator';
export interface KillSwithValidatorParams extends BaseValidatorAPIParams {
    guardian: Signer;
    delaySeconds: number;
}
export declare class KillSwitchValidator extends BaseValidatorAPI {
    guardian: Signer;
    delaySeconds: number;
    constructor(params: KillSwithValidatorParams);
    signer(): Promise<Signer>;
    getEnableData(): Promise<string>;
    signUserOp(userOperation: UserOperationStruct): Promise<string>;
    signMessage(message: Bytes | string): Promise<string>;
}

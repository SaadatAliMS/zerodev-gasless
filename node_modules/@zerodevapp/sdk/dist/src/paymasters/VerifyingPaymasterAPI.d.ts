import { UserOperationStruct } from '@zerodevapp/contracts';
import { PaymasterAPI } from './PaymasterAPI';
import { PaymasterProvider } from '../types';
export declare class VerifyingPaymasterAPI extends PaymasterAPI {
    readonly projectId: string;
    readonly paymasterUrl: string;
    readonly chainId: number;
    readonly entryPointAddress: string;
    readonly paymasterProvider?: PaymasterProvider | undefined;
    constructor(projectId: string, paymasterUrl: string, chainId: number, entryPointAddress: string, paymasterProvider?: PaymasterProvider | undefined);
    getPaymasterResp(userOp: Partial<UserOperationStruct>): Promise<object | undefined>;
}

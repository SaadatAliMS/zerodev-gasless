import { PaymasterProvider, SupportedGasToken } from '../types';
import { PaymasterAPI } from './PaymasterAPI';
export declare function getPaymaster(projectId: string, paymasterUrl: string, chainId: number, entryPointAddress: string, paymasterProvider?: PaymasterProvider, gasToken?: SupportedGasToken): Promise<PaymasterAPI>;

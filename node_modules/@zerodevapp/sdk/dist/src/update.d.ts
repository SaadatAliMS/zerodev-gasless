import { ContractTransaction } from 'ethers';
import { ZeroDevSigner } from './ZeroDevSigner';
export declare class UpdateController {
    readonly signer: ZeroDevSigner;
    updateAvailable: boolean;
    managerUpdateInfo?: {
        prev: string;
        oldManager: string;
        newManager: string;
    };
    singletonUpdateInfo?: {
        newSingleton: string;
    };
    constructor(signer: ZeroDevSigner);
    checkUpdate(latestAccountFactoryAddr: string): Promise<boolean>;
    update(): Promise<ContractTransaction | undefined>;
}
